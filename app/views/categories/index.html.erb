<%= render 'shared/content_title', title: 'All categories' %>

<% @categories.each do |category| %>
      <%=category.name%>
      <%= link_to 'Destroy', category_path(category), method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure?'} %>
<% end %>